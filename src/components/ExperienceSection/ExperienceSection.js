import React from 'react';

function ExperienceSection({ description }) {
  return (
    <section className="experience">
      <p>{description}</p>
    </section>
  );
}

export default ExperienceSection;